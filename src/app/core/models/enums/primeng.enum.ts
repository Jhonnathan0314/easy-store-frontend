export enum DateSelectionMode {
    SINGLE = "single",
    MULTIPLE = "multiple",
    RANGE = "range"
}

export enum DataViewLayout {
    LIST = "list",
    GRID = "grid"
}

export enum ButtonIconPosition {
    LEFT = "left",
    RIGHT = "right"
}